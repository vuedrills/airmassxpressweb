import 'package:flutter/material.dart';

import 'layouts/blogs_layout.dart';

class BlogsView extends StatelessWidget {
  const BlogsView({super.key});

  @override
  Widget build(BuildContext context) {
    return BlogsLayout();
  }
}
